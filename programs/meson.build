

psy_enum_fonts = executable (
    'psy-enum-fonts',
    files('psy-enum-fonts.c'),
    dependencies : [psy_dep]
)

psy_enum_parallel = executable (
    'psy-enum-parallel',
    files('psy-enum-parallel.c'),
    dependencies : [psy_dep]
)
